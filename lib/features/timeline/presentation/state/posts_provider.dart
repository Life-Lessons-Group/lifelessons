// import 'package:firebase_flutter_life/Models/post_model.dart';
// import 'package:flutter/foundation.dart';


// class PostProvider with ChangeNotifier {
//   //we use a getter to call a copy of the foodItems list, this ensures we can't edit on accident -> data is only changed inside of the class

//   // List<Post> get items {
//   //   return [..._items];
//   // }

//   //   List<Post> get favorites {
//   //   return _items.where((postItem) => postItem.isFavorite).toList();
//   // }


//   // FoodModel findByID(String id) {
//   //   return _items.firstWhere((prod) => prod.id == id);
//   // }
// }
